import React from 'react';
import Card from './Card';

const PhasesTimeline: React.FC = () => {
  return (
    <Card title="Fases do Projeto">
      <p>Este componente pode ser usado para exibir um cronograma das fases de desenvolvimento e implementação do sistema.</p>
      {/* Placeholder for a timeline component */}
    </Card>
  );
};

export default PhasesTimeline;
